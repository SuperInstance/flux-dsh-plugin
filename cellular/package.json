{
  "name": "flux-dsh-cellular",
  "version": "0.1.0",
  "private": true,
  "description": "The cellular seam: one DSH plugin granularized into a quilt organism of cells (perceive/compile/heal/run + germ). Branch experiment dsh-cellular-seam.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "demo": "tsx src/demo.ts"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-session": "0.1.1-rc.2",
    "@deepseek-ai/dsh-tools": "0.1.1-rc.2",
    "@types/node": "^22.0.0",
    "tsx": "^4.23.12",
    "typescript": "^5.6.0",
    "vitest": "^2.1.1"
  },
  "engines": {
    "node": ">=22"
  }
}
